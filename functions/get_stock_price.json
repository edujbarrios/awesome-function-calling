{
  "name": "get_stock_price",
  "description": "Returns the latest market price, daily change, and key statistics for a given stock ticker symbol.",
  "parameters": {
    "type": "object",
    "properties": {
      "symbol": {
        "type": "string",
        "description": "Stock ticker symbol, e.g. 'AAPL', 'TSLA', 'GOOGL', 'MSFT'."
      },
      "exchange": {
        "type": "string",
        "description": "Exchange identifier, e.g. 'NASDAQ', 'NYSE', 'LSE'. Inferred from symbol if omitted."
      },
      "include_history": {
        "type": "boolean",
        "description": "If true, includes 30-day closing price history in the response. Defaults to false.",
        "default": false
      }
    },
    "required": ["symbol"]
  }
}
